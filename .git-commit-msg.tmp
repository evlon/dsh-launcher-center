refactor(admin): 管理页脚本拆为 src/web/ 10 个职责模块，服务端按序拼接

原 admin.js 单文件 1174 行 / 75 个顶层声明平铺，无模块边界。现按职责拆分：
  core.js      全局状态 + 通用工具（esc/toast/fmtTime/cmpVer）
  auth.js      登录门禁 + Tab 切换
  config.js    中心配置 / 客户端默认值
  bridge.js    本机管理能力（外网代理网关）——含生产拓扑与状态诚实原则说明
  plugins.js   插件策略与同步——含「内网版本 vs npmjs 上游」语义警告
  npm.js       npm 包同步
  menu.js      菜单策略
  clients.js   客户端展示
  launcher.js  launcher 发布物
  boot.js      启动引导（必须最后）

装配方式：新增 src/adminScript.js，GET /admin.js 每次请求把 src/web/*.js
按序拼成一份脚本返回。因此：
  - 管理页 HTML 的 script src=/admin.js 契约不变
  - 部署链路（Dockerfile/k8s 重建脚本）无需新增静态路由
  - 源码即部署物，无生成文件、无「先构建再部署」，改模块刷新即生效
  - 已删除独立的 admin.js 文件

拆分正确性证据：
- tools/split-admin.cjs 用「按边界切段 + 按声明名分派」并断言
  「86 段拼接后可逐字节还原原文件」（纯搬运，零改写）
- 契约对比：顶层声明 75 个、DOM id 44 个、onclick 引用函数——
  拆分前后完全一致，无重复声明
- 语义对比：去注释空行后的 1057 行代码多重集合完全一致（仅状态声明位置集中）
- 新增 tests/adminscript.selftest.cjs（32 项：模块齐全/语法/顺序/契约/无重复）
- 新增 tests/smoke-live.cjs（15 项：真实进程拉 /admin /admin.js /download）
- 黄金基线 69 例：除 /admin.js 的 sha（预期变化，已论证安全）外零差异
- 全部测试：70 单测 + 32 装配 + 10 envDefaults + 15 冒烟 全过

部署链路同步：
- Dockerfile 去掉 COPY admin.js，改 COPY src/
- 三个 k8s 重建脚本（k8s/、k8s-desens/）打包 src/ 并逐个 node --check
